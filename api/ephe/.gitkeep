# Swiss Ephemeris data files (*.se1) are placed here at build time.
# They are gitignored (see root .gitignore) — fetch from the Swiss Ephemeris
# distribution during the Docker build / setup. This file keeps the dir tracked.
